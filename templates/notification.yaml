apiVersion: v1
kind: Service
metadata:
  labels:
    service: {{ .Values.notification.serviceName }}
  # namespace: {{ .Values.notification.nameSpace }}
  name: {{ .Values.notification.serviceName }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.notification.servicePort }}
      targetPort: {{ .Values.notification.servicePort }}
      nodePort: {{ .Values.notification.nodePort }}
      name: {{ .Values.notification.serviceName }}
  selector:
    service: {{ .Values.notification.serviceName }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: {{ .Values.notification.serviceName }}
  name: {{ .Values.notification.serviceName }}
data:
    application.refresh.cache.expiration: "0s"
spec:
  selector:
    matchLabels:
      service: {{ .Values.notification.serviceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.applicationName }}
        component: {{ .Values.notification.serviceName }}
        service: {{ .Values.notification.serviceName }}
    spec:
      containers:
      - name: {{ .Values.notification.serviceName }}
        image: "{{ .Values.notification.imageName }}:{{ .Values.notification.imageTag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        resources:
          requests:
            cpu: "{{ .Values.notification.requests.cpu }}"
            memory: "{{ .Values.notification.requests.memory }}"
          limits:
            cpu: "{{ .Values.notification.requests.cpu }}"
            memory: "{{ .Values.notification.requests.memory }}"
        env:
          - name: DB_HOST
            value: "{{ .Values.notification.environment.DB_HOST }}"
          - name: DB_PORT
            value: "{{ .Values.notification.environment.DB_PORT }}"
          - name: DB_PASS
            value: "{{ .Values.notification.environment.DB_PASS }}"
          - name: DB_USER
            value: "{{ .Values.notification.environment.DB_USER }}"
          - name: DB_NAME
            value: "{{ .Values.notification.environment.DB_NAME }}"
          - name: WILDFLY_USER
            value: "{{ .Values.notification.environment.WILDFLY_USER }}"
          - name: WILDFLY_PASS
            value: {{ .Values.notification.environment.WILDFLY_PASS }}
        ports:
        - containerPort: {{ .Values.notification.servicePort }}
      imagePullSecrets:
        - name: {{ .Values.pullSecret }}

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Values.notification.serviceName }}-hpa"
spec:
  minReplicas: {{ .Values.notification.minReplicas }}
  maxReplicas: {{ .Values.notification.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.notification.serviceName }}
  metrics:
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70

