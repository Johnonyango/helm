apiVersion: v1
kind: Service
metadata:
  labels:
    service: {{ .Values.xcbenefits.serviceName }}
  # namespace: {{ .Values.xcbenefits.nameSpace }}
  name: {{ .Values.xcbenefits.serviceName }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.xcbenefits.servicePort }}
      targetPort: {{ .Values.xcbenefits.servicePort }}
      nodePort: {{ .Values.xcbenefits.nodePort }}
      name: {{ .Values.xcbenefits.serviceName }}
  selector:
    service: {{ .Values.xcbenefits.serviceName }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: {{ .Values.xcbenefits.serviceName }}
  name: {{ .Values.xcbenefits.serviceName }}
spec:
  selector:
    matchLabels:
      service: {{ .Values.xcbenefits.serviceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.applicationName }}
        component: {{ .Values.xcbenefits.serviceName }}
        service: {{ .Values.xcbenefits.serviceName }}
    spec:
      containers:
      - name: {{ .Values.xcbenefits.serviceName }}
        image: "{{ .Values.xcbenefits.imageName }}:{{ .Values.xcbenefits.imageTag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        # resources:
        #   requests:
        #     cpu: "{{ .Values.xcbenefits.requests.cpu }}"
        #     memory: "{{ .Values.xcbenefits.requests.memory }}"
        #   limits:
        #     cpu: "{{ .Values.xcbenefits.requests.cpu }}"
        #     memory: "{{ .Values.xcbenefits.requests.memory }}"
        env:
          - name: DATABASE_URL
            value: "{{ .Values.xcbenefits.environment.DATABASE_URL }}"
          - name: DATABASE_PASSWORD
            value: "{{ .Values.xcbenefits.environment.DATABASE_PASSWORD }}"
          - name: DATABASE_USERNAME
            value: "{{ .Values.xcbenefits.environment.DATABASE_USERNAME }}"
          - name: WILDFLY_USERNAME
            value: "{{ .Values.xcbenefits.environment.WILDFLY_USERNAME }}"
          - name: WILDFLY_PASSWORD
            value: "{{ .Values.xcbenefits.environment.WILDFLY_PASSWORD }}"
        command: [ "sh", "-c", "/opt/jboss/wildfly/configure_wildfly.sh" ]
        ports:
        - containerPort: {{ .Values.xcbenefits.servicePort }}
      imagePullSecrets:
        - name: {{ .Values.pullSecret }}

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Values.xcbenefits.serviceName }}-hpa"
spec:
  minReplicas: {{ .Values.xcbenefits.minReplicas }}
  maxReplicas: {{ .Values.xcbenefits.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.xcbenefits.serviceName }}
  metrics:
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70

