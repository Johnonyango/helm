apiVersion: v1
kind: Service
metadata:
  labels:
    service: {{ .Values.xcpayroll.serviceName }}
  # namespace: {{ .Values.xcpayroll.nameSpace }}
  name: {{ .Values.xcpayroll.serviceName }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.xcpayroll.servicePort }}
      targetPort: {{ .Values.xcpayroll.servicePort }}
      nodePort: {{ .Values.xcpayroll.nodePort }}
      name: {{ .Values.xcpayroll.serviceName }}
  selector:
    service: {{ .Values.xcpayroll.serviceName }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: {{ .Values.xcpayroll.serviceName }}
  name: {{ .Values.xcpayroll.serviceName }}
spec:
  selector:
    matchLabels:
      service: {{ .Values.xcpayroll.serviceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.applicationName }}
        component: {{ .Values.xcpayroll.serviceName }}
        service: {{ .Values.xcpayroll.serviceName }}
    spec:
      containers:
      - name: {{ .Values.xcpayroll.serviceName }}
        image: "{{ .Values.xcpayroll.imageName }}:{{ .Values.xcpayroll.imageTag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        # resources:
        #   requests:
        #     cpu: "{{ .Values.xcpayroll.requests.cpu }}"
        #     memory: "{{ .Values.xcpayroll.requests.memory }}"
        #   limits:
        #     cpu: "{{ .Values.xcpayroll.requests.cpu }}"
        #     memory: "{{ .Values.xcpayroll.requests.memory }}"
        env:
          - name: DATABASE_URL
            value: "{{ .Values.xcpayroll.environment.DATABASE_URL }}"
          - name: DATABASE_PASSWORD
            value: "{{ .Values.xcpayroll.environment.DATABASE_PASSWORD }}"
          - name: DATABASE_USERNAME
            value: "{{ .Values.xcpayroll.environment.DATABASE_USERNAME }}"
          - name: WILDFLY_USERNAME
            value: "{{ .Values.xcpayroll.environment.WILDFLY_USERNAME }}"
          - name: WILDFLY_PASSWORD
            value: "{{ .Values.xcpayroll.environment.WILDFLY_PASSWORD }}"
        command: [ "sh", "-c", "/opt/jboss/wildfly/configure_wildfly.sh" ]
        ports:
        - containerPort: {{ .Values.xcpayroll.servicePort }}
      imagePullSecrets:
        - name: {{ .Values.pullSecret }}

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Values.xcpayroll.serviceName }}-hpa"
spec:
  minReplicas: {{ .Values.xcpayroll.minReplicas }}
  maxReplicas: {{ .Values.xcpayroll.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.xcpayroll.serviceName }}
  metrics:
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70

