apiVersion: v1
kind: Service
metadata:
  labels:
    service: {{ .Values.xc-payroll.serviceName }}
  # namespace: {{ .Values.xc-payroll.nameSpace }}
  name: {{ .Values.xc-payroll.serviceName }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.xc-payroll.servicePort }}
      targetPort: {{ .Values.xc-payroll.servicePort }}
      nodePort: {{ .Values.xc-payroll.nodePort }}
      name: {{ .Values.xc-payroll.serviceName }}
  selector:
    service: {{ .Values.xc-payroll.serviceName }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: {{ .Values.xc-payroll.serviceName }}
  name: {{ .Values.xc-payroll.serviceName }}
spec:
  selector:
    matchLabels:
      service: {{ .Values.xc-payroll.serviceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.applicationName }}
        component: {{ .Values.xc-payroll.serviceName }}
        service: {{ .Values.xc-payroll.serviceName }}
    spec:
      containers:
      - name: {{ .Values.xc-payroll.serviceName }}
        image: "{{ .Values.xc-payroll.imageName }}:{{ .Values.xc-payroll.imageTag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        # resources:
        #   requests:
        #     cpu: "{{ .Values.xc-payroll.requests.cpu }}"
        #     memory: "{{ .Values.xc-payroll.requests.memory }}"
        #   limits:
        #     cpu: "{{ .Values.xc-payroll.requests.cpu }}"
        #     memory: "{{ .Values.xc-payroll.requests.memory }}"
        env:
          - name: DATABASE_URL
            value: "{{ .Values.xc-payroll.environment.DATABASE_URL }}"
          - name: DATABASE_PASSWORD
            value: "{{ .Values.xc-payroll.environment.DATABASE_PASSWORD }}"
          - name: DATABASE_USERNAME
            value: "{{ .Values.xc-payroll.environment.DATABASE_USERNAME }}"
          - name: WILDFLY_USERNAME
            value: "{{ .Values.xc-payroll.environment.WILDFLY_USERNAME }}"
          - name: WILDFLY_PASSWORD
            value: "{{ .Values.xc-payroll.environment.WILDFLY_PASSWORD }}"
        command: [ "sh", "-c", "/opt/jboss/wildfly/configure_wildfly.sh" ]
        ports:
        - containerPort: {{ .Values.xc-payroll.servicePort }}
      imagePullSecrets:
        - name: {{ .Values.pullSecret }}

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Values.xc-payroll.serviceName }}-hpa"
spec:
  minReplicas: {{ .Values.xc-payroll.minReplicas }}
  maxReplicas: {{ .Values.xc-payroll.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.xc-payroll.serviceName }}
  metrics:
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70

