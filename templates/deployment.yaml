apiVersion: v1
kind: Service
metadata:
  labels:
    service: {{ .Values.nassitmss.serviceName }}
  name: {{ .Values.nassitmss.serviceName }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.nassitmss.servicePort }}
      targetPort: {{ .Values.nassitmss.servicePort }}
      nodePort: {{ .Values.nassitmss.nodePort }}
      name: {{ .Values.nassitmss.serviceName }}
  selector:
    service: {{ .Values.nassitmss.serviceName }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: {{ .Values.nassitmss.serviceName }}
  name: {{ .Values.nassitmss.serviceName }}
data:
    application.refresh.cache.expiration: "0s"
spec:
  selector:
    matchLabels:
      service: {{ .Values.nassitmss.serviceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.applicationName }}
        component: {{ .Values.nassitmss.serviceName }}
        service: {{ .Values.nassitmss.serviceName }}
    spec:
      containers:
      - name: {{ .Values.nassitmss.serviceName }}
        image: "{{ .Values.nassitmss.imageName }}:{{ .Values.nassitmss.imageTag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        # resources:
        #   requests:
        #     cpu: "{{ .Values.nassitmss.requests.cpu }}"
        #     memory: "{{ .Values.nassitmss.requests.memory }}"
        #   limits:
        #     cpu: "{{ .Values.nassitmss.requests.cpu }}"
        #     memory: "{{ .Values.nassitmss.requests.memory }}"
        env:
          - name: DB_HOST
            value: "{{ .Values.nassitmss.environment.DB_HOST }}"
          - name: DB_PORT
            value: "{{ .Values.nassitmss.environment.DB_PORT }}"
          - name: DB_PASS
            value: "{{ .Values.nassitmss.environment.DB_PASS }}"
          - name: DB_USER
            value: "{{ .Values.nassitmss.environment.DB_USER }}"
          - name: DB_NAME
            value: "{{ .Values.nassitmss.environment.DB_NAME }}"
          - name: WILDFLY_USER
            value: "{{ .Values.nassitmss.environment.WILDFLY_USER }}"
          - name: WILDFLY_PASS
            value: {{ .Values.nassitmss.environment.WILDFLY_PASS }}
        command: [ "sh", "-c", "/opt/jboss/wildfly/configure_wildfly.sh" ]
        ports:
        - containerPort: {{ .Values.nassitmss.servicePort }}
      imagePullSecrets:
        - name: {{ .Values.pullSecret }}

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: "{{ .Values.nassitmss.serviceName }}-hpa"
spec:
  minReplicas: {{ .Values.nassitmss.minReplicas }}
  maxReplicas: {{ .Values.nassitmss.maxReplicas }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.nassitmss.serviceName }}
  metrics:
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
